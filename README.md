# Numerical Illustrations for Dissertation

This repository contains the Python code used to generate the numerical figures appearing in my MSci Mathematics dissertation. 

Two figures are reproduced: 

1. Mean Squared Error (MSE) decay of the Bayesian estimator derived in Chapter 3.
2. Equilibrium curvature of payoff with respect to belief analysed in Chapter 4.

The scripts implement the theoretical expressions derived in the dissertation and generate the figures used in the numerical illustrations.

## Repsoitory Contents

```bash
MSE_decay.py
```
This script generates the figure illustrating the theoretical decay of the mean squared error

$$
\delta_R = \mathbb{E}[(\hat\rho_R - \rho^*)^2]
$$

as a function of the history of length $R$.

The plot illustrates the theoretical result derived in Chapter 3 (Statistical Properties of Bayesian Learning): 

$$
\delta_R \approx \frac{C_{Var}}{R},
$$

showing that estimation error declines at the parameteric rate 1/R.

The implementation uses example transition probabilities for the Markov chain: 

```bash
p_11 = 0.7
p_01 = 0.3
```
These determine the stationary distribution

$$
\pi_1 = \frac{p_{01}}{p_{01} + (1 - p_{11})}
$$

and the variance constant 

$$
C_{Var} = \frac{p_{11} (1-p_{11})}{\pi_1}.
$$

Running the script produces the file 

```bash
mse_decay_plot.png
```
which corresponds to the schematic illustration of MSE decay in Chapter 3.


## Requirements & Running the Code

The code requires the following Python packages:

```bash
numpy
matplotlib
```
Install them using: 

```bash
pip install numpy matplotlib
```

These can be installed using:

```bash
pip install numpy matplotlib
```

## Running the Code

To reproduce the MSE decay figure, run: 

```bash
python MSE_decay.py
```

To reproduce the equilibrium curvature figure, run:

```bash
python coefficient_plot.py
```

The script will generate the output file in the working directory.

## Relation to Dissertation 
The figurs generated by this repository correspond to the numerical illustrations used in the dissertaion:

##### Chapter 3: Statistical Properties of Bayesian Leanring
- The illustration of the theoretical $1/R$ decary of the mean squared error of the Bayesian estimator.

##### Chapter 4: Strategic Curvature and Informational Dominance 
- Numerical evaluation of the curvature coefficient and the payoff coefficient determining informational advantage.

## Author 

Alice Warren 
Fourth Year Mathematics Dissertation
2026
